<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NativeApp">

        <!-- A launcher start always comes back to the home screen. Without
             this, Android resumes the task where it left off, so a cold start
             after the search screen was open would begin there — the Flutter
             and Expo apps have a single activity and always start at home. -->
        <activity
            android:name=".MainActivity"
            android:clearTaskOnLaunch="true"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The search screen. -->
        <activity
            android:name=".RepoSearchActivity"
            android:exported="false" />
    </application>

</manifest>
